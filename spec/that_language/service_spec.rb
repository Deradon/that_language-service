require 'spec_helper'

describe ThatLanguage::Service do
  it 'has a version number' do
    expect(ThatLanguage::Service::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
